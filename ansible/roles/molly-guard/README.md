molly-guard
=========

Installs the `mollyguard` package to force typing in the hostname to avoid accidental reboots.


Example Playbook
----------------

```yml
- hosts: all
  roles:
     - molly-guard
```
